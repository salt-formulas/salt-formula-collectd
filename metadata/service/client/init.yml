applications:
- collectd
classes:
- service.collectd.support
parameters:
  collectd:
    client:
      enabled: true
      read_interval: 60
      write_queue_limit_high: 10000
      write_queue_limit_low: 10000
      use_fqdn: true
